import { HowView } from '@/components/HowView'

export const metadata = {
  title: 'Как это устроено — Арена',
  description: 'На каких переговорных методиках построена «Арена» и как считается результат партии.',
}

export default function HowPage() {
  return <HowView />
}
